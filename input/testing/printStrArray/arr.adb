procedure Print is 
   begin 
       main
       begin 
         a := newarray(10);
         array := write (a, 0, 72);
         array := write (a, 1, 69);
         array := write (a, 2, 76);
         array := write (a, 3, 76);
         array := write (a, 4, 79);
         array := write (a, 5, 79);
         array := write (a, 6, 10);
         array := write (a, 7, 0);
         Put_LineString(a);
       end; 
   end;